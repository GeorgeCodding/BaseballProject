# Plan:
- Figure out how to get team wins, and how I want to format this data.

- Build evaluation model which uses certain hitting, pitching, and fielding statistics to predict how much a Team won by.

- Create the script to grab those stats from the mlb api in the least queries, then turn them into a csv. Start very small with the data. 1 season.

- Label the data? This will be part of the model process. Do we tell the ML just that a team won or how much they won by. Is there something else that should fit in there. 

- Once the data is collected, work on model generation. Learn how to generate models. Different model building frameworks, what can I start with, what should I do from scratch.

- Generate initial models off of not much data.

- Test the models on other games. Preferably close games.


